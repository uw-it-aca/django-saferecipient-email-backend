#!/usr/bin/env python

from distutils.core import setup

setup(name='SafeEmailBackend',
      version='1.0',
      description='A django email backend that sends all emails to a given address',
     )
